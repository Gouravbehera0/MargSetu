# MARGSETU Environment Configuration
# Copy to .env for local development

# Frontend Configuration
VITE_API_URL=http://localhost:8000
VITE_OSRM_URL=https://router.project-osrm.org

# Backend Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=development
CORS_ORIGINS=*

# Optional PostgreSQL / Supabase Credentials (defaults to in-memory/sqlite if unset)
DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
